diff --git a/_bmad-output/implementation-artifacts/4-1-parseur-des-menaces-de-la-meta-meta-targets-yaml.md b/_bmad-output/implementation-artifacts/4-1-parseur-des-menaces-de-la-meta-meta-targets-yaml.md
index 43ea97c..20c6809 100644
--- a/_bmad-output/implementation-artifacts/4-1-parseur-des-menaces-de-la-meta-meta-targets-yaml.md
+++ b/_bmad-output/implementation-artifacts/4-1-parseur-des-menaces-de-la-meta-meta-targets-yaml.md
@@ -1,4 +1,5 @@
 ---
+baseline_commit: d98d8eec0716809ece00e8c3f3ce86f1bbe1c839
 # Story 4.1: Parseur des Menaces de la M├®ta (`meta_targets.yaml`)
 
 **Status:** ready-for-dev
@@ -45,21 +46,23 @@ So that le syst├¿me connaisse les interruptions majeures contre lesquelles il do
 - Il est crucial de s├®parer la logique de parsing (Lecture de fichier + validation Pydantic) du moteur d'ex├®cution (Epic 4, Story 4.2). Cette Story 4.1 ne s'occupe QUE de la lecture et de la validation des donn├®es en m├®moire.
 
 ## Tasks / Subtasks
-- [ ] Cr├®er les mod├¿les Pydantic pour les menaces (`MetaThreat`).
-- [ ] ├ëcrire la fonction de parsing `load_meta_targets` (qui cr├®e un fichier par d├®faut s'il est absent).
-- [ ] Ajouter les messages d'erreurs robustes utilisant `rich.console`.
-- [ ] ├ëcrire les tests unitaires couvrant les cas nominaux et d'erreurs.
+- [x] Cr├®er les mod├¿les Pydantic pour les menaces (`MetaThreat`).
+- [x] ├ëcrire la fonction de parsing `load_meta_targets` (qui cr├®e un fichier par d├®faut s'il est absent).
+- [x] Ajouter les messages d'erreurs robustes utilisant `rich.console`.
+- [x] ├ëcrire les tests unitaires couvrant les cas nominaux et d'erreurs.
 
 ## Dev Agent Record
-- **Implementation Notes:** (To be filled by the dev agent)
-- **Tests Added:** (To be filled by the dev agent)
+- **Implementation Notes:** Impl├®mentation des mod├¿les `MetaThreat` et `MetaTargetsRequest` dans `models/meta.py` avec `ConfigDict(extra='forbid')` pour garantir un typage strict. Le parseur dans `rules/meta_parser.py` intercepte `yaml.YAMLError` et `pydantic.ValidationError`, et affiche des messages explicites via Rich avant d'ex├®cuter un `typer.Exit(code=1)`. Le fichier par d├®faut est cr├®├® correctement s'il n'existe pas.
+- **Tests Added:** Ajout de `test_meta_parser.py` avec 5 tests couvrant : parsing valide, cr├®ation du fichier par d├®faut, gestion d'erreurs YAML (YAMLError), gestion d'erreurs de sch├®ma Pydantic (ValidationError), et rejet de champs extra (forbid extra).
 
 ### File List
-- (To be filled by the dev agent)
+- `src/ygo_ultime_deck/models/meta.py` (NEW)
+- `src/ygo_ultime_deck/rules/meta_parser.py` (NEW)
+- `tests/test_rules/test_meta_parser.py` (NEW)
 
 ## Change Log
-- (To be filled by the dev agent)
+- Ajout de la gestion et du parsing du fichier de configuration `meta_targets.yaml`.
 
 ## Completion Status
-- **Status Update:** ready-for-dev
+- **Status Update:** review
 - **Note:** Initial story definition completed.
diff --git a/_bmad-output/implementation-artifacts/sprint-status.yaml b/_bmad-output/implementation-artifacts/sprint-status.yaml
index abe49ab..6246d7f 100644
--- a/_bmad-output/implementation-artifacts/sprint-status.yaml
+++ b/_bmad-output/implementation-artifacts/sprint-status.yaml
@@ -60,7 +60,7 @@ development_status:
   3-4-resolution-des-resource-generators-pioche-dynamique: done
   epic-3-retrospective: optional
   epic-4: in-progress
-  4-1-parseur-des-menaces-de-la-meta-meta-targets-yaml: ready-for-dev
+  4-1-parseur-des-menaces-de-la-meta-meta-targets-yaml: review
   4-2-simulation-de-l-immunite-counter-matching: backlog
   4-3-optimiseur-dynamique-de-taille-de-deck-40-a-60-cartes: backlog
   4-4-module-d-audit-cli-et-exportation-ydk: backlog
diff --git a/diff_4_1.txt b/diff_4_1.txt
new file mode 100644
index 0000000..8edfc35
Binary files /dev/null and b/diff_4_1.txt differ
diff --git a/src/ygo_ultime_deck/models/meta.py b/src/ygo_ultime_deck/models/meta.py
new file mode 100644
index 0000000..36bace9
--- /dev/null
+++ b/src/ygo_ultime_deck/models/meta.py
@@ -0,0 +1,12 @@
+from typing import List, Optional
+from pydantic import BaseModel, Field, ConfigDict
+
+class MetaThreat(BaseModel):
+    model_config = ConfigDict(extra='forbid')
+    name: str = Field(min_length=1)
+    category: Optional[str] = Field(default="Generic")
+    count: int = Field(default=1, ge=1)
+
+class MetaTargetsRequest(BaseModel):
+    model_config = ConfigDict(extra='forbid')
+    threats: List[MetaThreat] = Field(min_length=1)
diff --git a/src/ygo_ultime_deck/rules/meta_parser.py b/src/ygo_ultime_deck/rules/meta_parser.py
new file mode 100644
index 0000000..da1a1b0
--- /dev/null
+++ b/src/ygo_ultime_deck/rules/meta_parser.py
@@ -0,0 +1,70 @@
+import yaml
+import typer
+from pathlib import Path
+from pydantic import ValidationError
+from rich.console import Console
+
+from ygo_ultime_deck.models.meta import MetaTargetsRequest, MetaThreat
+
+console = Console(stderr=True)
+
+DEFAULT_META_CONTENT = """# Fichier de configuration des menaces M├®ta (meta_targets.yaml)
+# D├®finissez ici les staples ou interruptions adverses.
+threats:
+  - name: "Ash Blossom & Joyous Spring"
+    category: "Handtrap"
+    count: 3
+  - name: "Infinite Impermanence"
+    category: "Handtrap"
+    count: 3
+  - name: "Nibiru, the Primal Being"
+    category: "Handtrap"
+    count: 1
+"""
+
+def load_meta_targets(filepath: Path | str) -> MetaTargetsRequest:
+    """
+    Load meta targets from a YAML file.
+    Creates a default configuration file if it doesn't exist.
+    
+    Args:
+        filepath: Path to the YAML file.
+        
+    Returns:
+        A validated MetaTargetsRequest object.
+        
+    Raises:
+        typer.Exit(1): If the file is invalid YAML, violates schema, or cannot be read.
+    """
+    path = Path(filepath)
+    
+    if not path.exists():
+        try:
+            path.parent.mkdir(parents=True, exist_ok=True)
+            path.write_text(DEFAULT_META_CONTENT, encoding="utf-8")
+        except PermissionError:
+            console.print(f"[bold red]Erreur critique : Permission refus├®e pour cr├®er le fichier {path}.[/bold red]")
+            raise typer.Exit(code=1)
+        except OSError as e:
+            console.print(f"[bold red]Erreur critique lors de la cr├®ation du fichier {path} :[/bold red]\n{e}")
+            raise typer.Exit(code=1)
+        
+    try:
+        with open(path, "r", encoding="utf-8") as f:
+            data = yaml.safe_load(f)
+            
+        if data is None:
+            console.print(f"[bold red]Erreur critique : Le fichier {path} est vide.[/bold red]")
+            raise typer.Exit(code=1)
+            
+        return MetaTargetsRequest(**data)
+        
+    except PermissionError:
+        console.print(f"[bold red]Erreur critique : Permission refus├®e pour lire le fichier {path}.[/bold red]")
+        raise typer.Exit(code=1)
+    except yaml.YAMLError as e:
+        console.print(f"[bold red]Erreur critique lors de l'analyse du fichier YAML {path} :[/bold red]\n{e}")
+        raise typer.Exit(code=1)
+    except ValidationError as e:
+        console.print(f"[bold red]Erreur de validation de sch├®ma dans {path} :[/bold red]\n{e}")
+        raise typer.Exit(code=1)
diff --git a/tests/test_rules/test_meta_parser.py b/tests/test_rules/test_meta_parser.py
new file mode 100644
index 0000000..1dc4150
--- /dev/null
+++ b/tests/test_rules/test_meta_parser.py
@@ -0,0 +1,64 @@
+import pytest
+import yaml
+from pathlib import Path
+from typer import Exit
+from ygo_ultime_deck.rules.meta_parser import load_meta_targets
+
+def test_load_meta_targets_valid(tmp_path):
+    yaml_file = tmp_path / "meta_targets.yaml"
+    content = """
+threats:
+  - name: "Ash Blossom"
+    category: "Handtrap"
+    count: 3
+"""
+    yaml_file.write_text(content, encoding="utf-8")
+    
+    result = load_meta_targets(yaml_file)
+    assert len(result.threats) == 1
+    assert result.threats[0].name == "Ash Blossom"
+    assert result.threats[0].category == "Handtrap"
+    assert result.threats[0].count == 3
+
+def test_load_meta_targets_creates_default(tmp_path):
+    yaml_file = tmp_path / "meta_targets_default.yaml"
+    assert not yaml_file.exists()
+    
+    result = load_meta_targets(yaml_file)
+    assert yaml_file.exists()
+    assert len(result.threats) > 0
+
+def test_load_meta_targets_invalid_yaml(tmp_path):
+    yaml_file = tmp_path / "meta_targets_invalid.yaml"
+    yaml_file.write_text("invalid: yaml: :", encoding="utf-8")
+    
+    with pytest.raises(Exit) as exc_info:
+        load_meta_targets(yaml_file)
+    assert exc_info.value.exit_code == 1
+
+def test_load_meta_targets_validation_error(tmp_path):
+    yaml_file = tmp_path / "meta_targets_bad_schema.yaml"
+    # Missing required 'name' in threat
+    content = """
+threats:
+  - count: 3
+"""
+    yaml_file.write_text(content, encoding="utf-8")
+    
+    with pytest.raises(Exit) as exc_info:
+        load_meta_targets(yaml_file)
+    assert exc_info.value.exit_code == 1
+
+def test_load_meta_targets_forbid_extra(tmp_path):
+    yaml_file = tmp_path / "meta_targets_extra.yaml"
+    # Extra field 'invalid_field'
+    content = """
+threats:
+  - name: "Ash Blossom"
+    invalid_field: "This should fail"
+"""
+    yaml_file.write_text(content, encoding="utf-8")
+    
+    with pytest.raises(Exit) as exc_info:
+        load_meta_targets(yaml_file)
+    assert exc_info.value.exit_code == 1
